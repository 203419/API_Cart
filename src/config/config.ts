import dotenv from 'dotenv';

dotenv.config();

export const config = {
  port: 3000, 
  dbUser: 'root',
  dbPassword: 'sebasramirez11',
  dbHost: 'localhost',
  dbName: 'disorders',
  dbPort: 3306,
};
